{
  "name": "clearterms-extension",
  "version": "0.1.0",
  "private": true,
  "description": "ClearTerms Chrome extension — detect TOS pages and analyze them.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
